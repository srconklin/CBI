{
  "name": "cbi",
  "version": "1.0.0",
  "description": "cbi algolia powered venue",
  "scripts": {
    "start": "export NODE_ENV=development&&parcel ./src/styles.css ./src/app.js",
    "build": "export NODE_ENV=production&&rm -rf dist .cache .parcel-cache && parcel build ./src/index.html --no-source-maps && cp –Rf /controllers /dist"
  },
  "browserslist": "> 0.2%",
  "devDependencies": {
    "autoprefixer": "^10.3.3",
    "parcel": "^2.0.0-rc.0"
  },
  "dependencies": {
    "alpinejs": "^2.8.2"
  }
}
