{
  "name": "cbi",
  "version": "1.0.0",
  "description": "cbi algolia powered venue",
  "scripts": {
    "start": "set NODE_ENV=development && parcel ./src/styles.js ./src/app.js",
    "build": "set NODE_ENV=production && rm -rf dist .cache .parcel-cache && parcel build ./src/index.html --no-source-maps"
  },
  "browserslist": "> 0.2%",
  "devDependencies": {
    "parcel": "^2.4.1"
  },
  "dependencies": {
    "alpinejs": "^2.8.2"
  }
}
